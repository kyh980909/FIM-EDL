# @package _global_

experiment:
  name: iedl_ref
  suite: cifar_ood
  method_variant: iedl_ref
  wandb_tags: ["method:iedl_ref", "fisher:i_mse", "pac_bayes:kl"]

loss:
  name: iedl_ref
  lambda_kl: -1.0
  lambda_logdet: 0.05
  fisher_c: 0.05
  kl_anneal_epochs: 10
  anneal_epochs: 10
